Fix: open miro_client.py and find the status-code check inside create_sticky_note (the one raising the RuntimeError you're seeing) and the identical check inside create_text_item. Change the failure condition from "status code is not 200" to "status code is not 200 and not 201" — or more robustly, "status code is 300 or higher" (any 2xx should be treated as success). The read function's check can stay as-is since GET does correctly return 200.